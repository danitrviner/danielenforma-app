/// <reference types="vite/client" />

// Inyectada por Vite en tiempo de build (ver `define` en vite.config.ts) con
// el commit del que sale el despliegue. La usa `src/monitorizacion.ts` para
// etiquetar cada error con la versión exacta en la que apareció.
declare const __APP_RELEASE__: string;
